name: logtest2
on: push
jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 7ad484fd
      - run: git log --format=%B -n1 HEAD^..HEAD 2>/dev/null || git log --format=%B -n1
